{
  "name": "chip-8-emu",
  "version": "1.0.0",
  "description": "Chip 8 Emulator",
  "main": "index.ts",
  "scripts": {
    "start": "node index.js",
    "build": "rm -rf build/ && tsc -p tsconfig.json && cp -r roms build/roms && cp src/index.html build"
  },
  "keywords": [
    "chip",
    "chip-8",
    "emulator"
  ],
  "author": "Kyle Somogyi",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.7.10",
    "typescript": "^4.4.2"
  }
}
